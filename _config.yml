# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Arkadij Kummer
email: arkadij dot kummer at gmail dot com
description: >- # this means to ignore newlines until "baseurl:"
    Biological data scientist turned ML engineer.
baseurl: "/blog" # the subpath of your site, e.g. /blog
url: "https://elarkk.github.io" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: ElArkkk
github_username: ElArkk

# Staticman & reCAPTCHA
# Set this to your Modal web endpoint base URL (no trailing slash)
staticman_endpoint: "https://elarkk--staticman-elarkk-fastapi-app.modal.run"
recaptcha:
  enabled: false
  # Public site key (not secret)
  site_key: "REPLACE_WITH_RECAPTCHA_SITE_KEY"
  # Encrypted secret (must be encrypted with Staticman's RSA public key)
  encrypted_secret: "REPLACE_WITH_ENCRYPTED_SECRET"

# Build settings
theme: minima
plugins:
    - jekyll-feed
    - jekyll-responsive-image
    - jekyll-seo-tag
    - jekyll-sitemap
    - jekyll-paginate

highlighter: rouge
markdown: kramdown
sass:
  quiet_deps: true
kramdown:
  input: GFM
  syntax_highlighter: rouge

# Include pages directory
include:
  - _pages

# Permalink structure
permalink: /:year/:month/:day/:title/

# Pagination
paginate: 10
paginate_path: "/page:num/"
# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# Exclude from processing
exclude:
  - .sass-cache/
  - .jekyll-cache/
  - gemfiles/
  - Gemfile
  - Gemfile.lock
  - node_modules/
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
  - package.json
  - package-lock.json
  - .gitignore
  - .gitattributes
  - README.md
  - CLAUDE.md
  - .pixi/
